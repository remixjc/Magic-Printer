{
  "name": "@magic-printer/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc -p tsconfig.json", "typecheck": "tsc -p tsconfig.json --noEmit", "test": "pnpm build && node --test dist/index.test.js", "lint": "tsc -p tsconfig.json --noEmit" },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/multipart": "^9.0.3",
    "@fastify/static": "^8.1.0",
    "@magic-printer/converters": "workspace:*",
    "@magic-printer/platform": "workspace:*",
    "@magic-printer/shared": "workspace:*",
    "fastify": "^5.2.1"
  }
}
